name: Keep Render Alive

on:
  schedule:
    - cron: '0,9,18,27,36,45,54 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl --silent --max-time 30 "https://jeuxvideo.onrender.com/ping"
          echo "✅ Pinged at $(date)"
